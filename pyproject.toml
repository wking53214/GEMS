[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gems-infrastructure"
version = "0.1.0"
description = "Evidence-preserving multi-Gem orchestration baseline reconstructed from GEMS-FER-1.0 evidence."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
